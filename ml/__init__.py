"""Multi-Disease AI — reusable ML package.

Sub-packages:
    data           dataset acquisition, caching and disease-specific loaders
    eda            exploratory data analysis utilities (profiling + plotting)
    preprocessing  leakage-safe sklearn preprocessing pipelines
    training       model zoo, hyper-parameter spaces, CV experiment runner
    evaluation     metrics, curves, calibration analysis
    explainability SHAP global/local explanation helpers
    fairness       subgroup performance analysis
    external       cross-dataset (no-retrain) validation
"""
