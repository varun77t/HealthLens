# Multi-Disease AI — pinned dependency set (verified working 2026-09-02, Python 3.12.6)
#
# NOTE: installing `shap` pulls in `numba`, which requires numpy >= 2.1. Install this
# project into its own virtual environment (`.venv/`) so numpy 2.x stays isolated from
# any global packages that pin numpy < 2 (e.g. TensorFlow, mediapipe).

# --- core data / ML ---
numpy==2.5.2
pandas==2.3.0
scipy==1.16.2
scikit-learn==1.7.2
imbalanced-learn==0.14.0
xgboost==3.0.5
lightgbm==4.6.0

# --- explainability ---
shap==0.52.0

# --- document generation & extraction (Phase 10) ---
# reportlab writes the synthetic demonstration reports; pdfplumber reads them back with
# word-level positions, which is what makes label -> value matching on a two-column lab
# report robust and supplies the "extracted from page 1" provenance. Both are pure Python:
# no system binaries, so nothing to install outside the venv.
reportlab==5.0.1
pdfplumber==0.11.10
# FastAPI needs this to accept multipart file uploads.
python-multipart==0.0.32

# --- dataset acquisition ---
ucimlrepo==0.0.7

# --- visualisation ---
matplotlib==3.10.3
seaborn==0.13.2

# --- serialization ---
joblib==1.5.2

# --- backend (used from Phase 8) ---
fastapi==0.115.0
uvicorn==0.30.6
pydantic==2.9.1

# --- notebooks & tests ---
jupyter==1.1.1
jupyterlab==4.6.3
ipykernel==7.3.0
nbconvert==7.17.1
notebook==7.6.2
pytest==9.1.1
